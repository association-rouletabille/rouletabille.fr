
<div webc:root="override">
  <span class="large">404</span>
  <svg>
    <use href="/assets/sprites.svg#icon-unicycle" />
  </svg>
  <span class="small">Cette page n'existe pas 😢</span>
</div>

<style webc:scoped>
:host {
  height: 100vh;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
}

:host svg {
  height: min(15vh, 15vw);
  transform: rotate(-138deg);
}

.large {
  font-weight: bold;
  font-size: min(20vh, 20vw);
}

.small {
  padding-top: min(20vh, 20vw);
}
</style>
