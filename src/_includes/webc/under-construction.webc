<div webc:root="override">
  <rocking-logo class="logo"></rocking-logo>
  <links></links>
</div>

<style webc:scoped>
.logo {
  width: min(70cqi, 70svh);
  height: min(70cqi, 70svh);
  padding-bottom: 5rem;
  z-index: -1;
}

:host {
  display: flex;
  flex-direction: column;
  place-content: stretch center;
  min-height: 100svh;
  container-type: inline-size;
}
</style>
