<div webc:root="override">
  <rocking-logo class="logo"></rocking-logo>
  <links></links>
</div>

<style webc:scoped>
.logo {
  width: min(70vh, 70vw);
  height: min(70vh, 70vw);
  padding-bottom: 10vh;
  z-index: -1;
}

:host {
  display: flex;
  flex-direction: column;
  place-content: stretch center;
  height: 100vh;
}
</style>
