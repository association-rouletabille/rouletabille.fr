<section webc:root="override">
  <h2 webc:if="this.heading" @text="this.heading"></h2>
  <slot></slot>
</section>

<style webc:scoped>
  :host {
    display: block;
    margin-block: 2.5rem;
    background-color: rgb(var(--palette-content-surface));
    border-radius: 0.75rem;
    padding: 1.25rem 1.5rem;
  }

  main :host:first-child {
    margin-block-start: 0;
  }

  :host > h2 {
    font-family: var(--font-mono);
    font-size: var(--fs-lg);
    font-weight: bold;
    color: rgb(var(--palette-accent));
    margin-block: 0 0.75rem;
    line-height: 1.3;
  }
</style>
