<!-- Top bar — cherry red, navigation links + social icons -->
<nav class="topbar" aria-label="Navigation principale">
  <ul class="nav-row">
    <li><a href="/initiations-monocycle-gratuites">Initiations gratuites</a></li>
    <li><a href="/balades-du-mardi-soir">Balades du Mardi Soir</a></li>
    <li><a href="/trial-street-flat">Trial, street et flat</a></li>
    <li><a href="/roule-ton-caillou">Roule Ton Caillou</a></li>
  </ul>
  <div class="nav-row-with-social">
    <ul class="nav-row">
      <li><a href="/association">L'association</a></li>
      <li><a href="/adhesion">Adh&eacute;rer</a></li>
      <li><a href="/tutoriels-et-resources">Tutoriels et resources</a></li>
    </ul>
    <div class="nav-spacer"></div>
    <div class="social-links">
      <a href="mailto:association.rouletabille@gmail.com?subject=Contact%20depuis%20le%20site%20web" aria-label="Email">
        <icon name="social-email"></icon>
      </a>
      <a href="https://www.instagram.com/rtb_monocycle" target="_blank" rel="noopener noreferrer" aria-label="Instagram">
        <icon name="social-instagram"></icon>
      </a>
      <a href="https://www.facebook.com/rouletabillemonocycleparis" target="_blank" rel="noopener noreferrer" aria-label="Facebook">
        <icon name="social-facebook"></icon>
      </a>
    </div>
  </div>
</nav>

<!-- Cream strip (decorative, no content) -->
<div class="subbar"></div>

<style webc:scoped>
  /* =====================
   Header: cherry top bar + red triangle (top-left) + cream bar
   Triangle clip-path controlled via --triangle-width / --triangle-height.
   Cream bar: horizontal subbar (grid row 2) + diagonal pseudo along hypotenuse.
   ===================== */

  :host {
    display: grid;
    grid-template-columns: 1fr;
    grid-template-rows: auto var(--bar-height);
    width: 100%;
    container-type: inline-size;
    position: relative;
    overflow: visible;
  }

  /* ---- Red triangle (top-left corner, extends below header) ---- */

  :host::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: var(--triangle-width);
    height: var(--triangle-height);
    background-color: rgb(var(--color-topbar-bg));
    clip-path: polygon(0 0, 100% 0, 0 100%);
    z-index: 0;
    pointer-events: none;
  }

  /* ---- Diagonal cream bar along the hypotenuse ---- */

  :host::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: calc(var(--triangle-width) + var(--bar-height));
    height: calc(var(--triangle-height) + var(--bar-height));
    background-color: rgb(var(--color-subbar-bg));
    clip-path: polygon(var(--triangle-width) 0, 0 var(--triangle-height), 0 calc(var(--triangle-height) + var(--bar-height)), calc(var(--triangle-width) + var(--bar-height)) 0);
    z-index: 1;
    pointer-events: none;
  }

  /* ---- Top bar (cherry red, full width) ---- */

  .topbar {
    grid-row: 1;
    grid-column: 1;
    display: grid;
    grid-template-columns: fit-content(100%);
    justify-content: end;
    align-content: center;
    row-gap: 0;
    background-color: rgb(var(--color-topbar-bg));
    padding-block: 0.25rem;
    padding-inline: 1.5rem;
    padding-inline-end: max(1.5rem, calc((100cqi - 1200px) / 2 + 1.5rem));
    position: relative;
    z-index: 2;
  }

  .nav-row {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-wrap: wrap;
    gap: 0.25rem 1.5rem;
    justify-content: flex-start;
  }

  .nav-row a {
    color: rgb(var(--color-topbar-text));
    font-family: var(--font-body);
    font-weight: 700;
    font-size: var(--fs-sm);
    text-transform: uppercase;
    text-decoration: none;
    letter-spacing: 0.04em;
    padding: 0;
    transition:
      color 0.2s ease,
      text-decoration-color 0.2s ease;
    text-decoration-line: underline;
    text-decoration-color: transparent;
    text-underline-offset: 0.25em;
    text-decoration-thickness: 2px;
  }

  .nav-row a:hover {
    color: rgb(var(--color-topbar-text-hover));
    text-decoration-color: rgb(var(--color-topbar-text-hover));
  }

  /* ---- Second nav row + social links ---- */

  .nav-row-with-social {
    display: flex;
    align-items: center;
  }

  .nav-spacer {
    flex: 1;
  }

  .social-links {
    display: flex;
    gap: 0.75rem;
    align-items: center;
    flex-shrink: 0;
  }

  .social-links a {
    display: flex;
    color: rgb(var(--color-topbar-text));
    text-decoration: none;
    transition: color 0.2s ease;
  }

  .social-links a:hover {
    color: rgb(var(--color-topbar-text-hover));
  }

  .social-links a svg {
    display: block;
    width: 1.1rem;
    height: 1.1rem;
  }

  /* ---- Cream bar — horizontal portion (grid row 2) ---- */

  .subbar {
    grid-row: 2;
    grid-column: 1;
    background-color: rgb(var(--color-subbar-bg));
    height: var(--bar-height);
    position: relative;
    z-index: 2;
  }

  /* ---- Mobile ---- */

  @media (max-width: 1023px) {
    :host {
      --triangle-width: 10rem;
      --triangle-height: 16rem;
    }

    .topbar {
      justify-content: start;
      padding-inline: 1rem;
    }
  }
</style>
